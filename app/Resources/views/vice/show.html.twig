{% extends 'base.html.twig' %}

{% block body %}
    <h1>Vice</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vice.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ vice.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('vice_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('vice_edit', { 'id': vice.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
